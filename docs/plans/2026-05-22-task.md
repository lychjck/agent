# ETF 持仓助手升级项目进度追踪

- [ ] **阶段一：依赖安装与 MCP 层统计学补强**
    - [x] 使用 `uv` 安装 `langgraph` 依赖包
    - [ ] 改造 `stock_get_holding_technical` 工具，实现历史日振幅标准差 ($\sigma$) 和偏离度 $Z-Score$ 的数理指标输出
    - [ ] 新增 `stock_get_etf_constituents` 工具，支持获取 ETF 的前十大重仓股数据
- [ ] **阶段二：策略引擎模块 (Strategy Engine) 独立解耦开发**
    - [ ] 设计策略引擎配置结构，允许在 `config.toml` 中录入网格与红利对冲参数
    - [ ] 实现 `grid.py` 经典网格纯数理计算规则
    - [ ] 实现 `dividend_hedge.py` 红利对冲纯数理计算规则
    - [ ] 实现 `engine.py` 策略核心路由与硬信号统一汇总输出
- [ ] **阶段三：LangGraph 智能决策大脑重构**
    - [ ] 定义 `state.py`，设计包含持仓、异常队列、策略硬信号、吹哨人警告等字段的 GraphState
    - [ ] 实现 `Anomaly Assessor Node`，基于 $Z-Score > 2.0$ 动态将标的推进待探案队列
    - [ ] 实现 `Investigator Node`，联动重仓股工具与 `opencli` 检索，追溯波动根因
    - [ ] 实现 `Strategy Auditor Node`，进行“AI 吹哨人审查”，在数理信号旁输出主观警示
    - [ ] 使用 `StateGraph` 装配 Workflow，并配置在策略审查后、最终报告生成前的 **Human-in-the-Loop 中断挂起** 机制
- [ ] **阶段四：验证与联调**
    - [ ] 编写并运行策略引擎纯数理单元测试
    - [ ] 模拟异常破位与大跌场景，端到端演练 LangGraph 的探案、挂起确认与警告输出逻辑
